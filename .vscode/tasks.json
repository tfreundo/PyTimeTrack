{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "code coverage",
            "type": "shell",
            "command": "pipenv run python -m coverage run -m unittest;pipenv run python -m coverage report --show-missing"
        }
    ]
}